language: ruby
sudo: false
before_install:
  - gem install bundler
  - gem install rails
cache: bundler
rvm:
  - 2.2.0
  - 2.3.0
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
  fast_finish: true
